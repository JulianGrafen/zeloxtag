"use client";

/**
 * Legacy grid gallery — public showcase uses the swipeable hero instead.
 * Re-export helpers for shared photo filtering.
 */
export {
  filterVisibleGalleryPhotos,
  photoUsesContainLayout,
  resolveHeroPhotoIndex,
} from "./showcase-gallery-photos";
